
//pragma options "--bnd-inbits 2";

int spec( int x, int y ){
   return (x + x) * (y + 5);
   //return ((33-x) + (33-x)) * ((33-y) + 5);
}

int sketch( int x, int y ) implements spec{
   return ( ?? * x + ?? ) * ( ?? * y + ??);
   //return ( ?? * (33-x) + ?? ) * ( ?? * (33-y) + ??);
}

