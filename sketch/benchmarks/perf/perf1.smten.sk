
bit reverseSketch(bit in) implements reverse {	
    return in;
}

bit reverse (bit in) {
	int[2] tmp = {0, 1};

    int idx = in ? 1 : 0;
    int v = tmp[idx];
	bit output;
	output = v == 1;
    v = 0;
	return output;
}

