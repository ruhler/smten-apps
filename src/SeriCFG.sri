
module SeriCFG where

import RegEx

type ID = String

data CFG =
     EpsilonC       -- matches ""
   | EmptyC         -- never matches
   | AtomC Elem
   | RangeC Elem Elem
   | StarC CFG
   | ConcatC CFG CFG
   | OrC CFG CFG
   | VariableC ID
   | FixC ID Integer
   deriving (Eq, Show)

